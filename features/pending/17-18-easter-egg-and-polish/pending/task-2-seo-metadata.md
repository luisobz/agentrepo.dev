# Task 2: SEO Metadata & Open Graph

## Descripción
Configurar el SEO vital para un proyecto público.

## Criterios de finalización
- [ ] Modificar todos los `layout.tsx` y `page.tsx` para usar la API de metadatos de Next.js (`export const metadata`).
- [ ] Añadir títulos dinámicos, descripciones cortas y tags de Open Graph para previsualizaciones en redes.
- [ ] Añadir un icono SVG dinámico o estático (`favicon.ico`, `apple-touch-icon`).
